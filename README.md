# ComfyUI Mask Blend Node

ComfyUI自定义节点包，提供图像序列处理功能。

## 节点功能

- **Mask Gradient (渐变过渡)** - 为图像序列创建从黑到白的mask渐变效果，可指定起始帧和结束帧
- **Frame Slice (帧切片)** - 从图像序列中提取指定范围的帧
- **Sequence Overlay (序列叠加)** - 将两段图像序列按mask叠加，支持指定叠加起始位置

## 安装

将此仓库克隆到 `ComfyUI/custom_nodes` 目录，然后重启ComfyUI。
